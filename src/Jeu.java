//Gestion du jeu


import Combat.*;
import Character.Enemy;
import Character.Hero;

public class Jeu {
    // Votre logique de jeu ici
    
    public void demarrer() {
        // Démarrez le jeu ici
        /*Hero guerrier = GestionnaireHeros.creerGuerrier();
        Enemy bandit = EnemyFactory.createBandit();
        Combat combat = new Combat(guerrier, bandit);
        combat.initCombat();
        System.out.println(guerrier.getHealthPoints());
        System.out.println(bandit.getHealthPoints());
        while(guerrier.isAlive() && bandit.isAlive()){
            combat.nextTurn(AttackType.HEAVY);
            System.out.println(guerrier.getHealthPoints());
            System.out.println(bandit.getHealthPoints());
        }

        Enemy zombie = EnemyFactory.createZombie();
        combat = new Combat(guerrier, zombie);
        combat.initCombat();
        System.out.println(guerrier.getHealthPoints());
        System.out.println(zombie.getHealthPoints());
        while(guerrier.isAlive() && zombie.isAlive()){
            combat.nextTurn(AttackType.HEAVY);
            System.out.println(guerrier.getHealthPoints());
            System.out.println(zombie.getHealthPoints());
        }

        Enemy wolf = EnemyFactory.createWolf();
        combat = new Combat(guerrier, wolf);
        combat.initCombat();
        System.out.println(guerrier.getHealthPoints());
        System.out.println(zombie.getHealthPoints());
        while(guerrier.isAlive() && wolf.isAlive()){
            combat.nextTurn(AttackType.HEAVY);
            System.out.println(guerrier.getHealthPoints());
            System.out.println(wolf.getHealthPoints());
        }*/
    }
}
